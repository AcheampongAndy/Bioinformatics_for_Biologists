#!/usr/bin/env bash

# To assign a variable, we use the = symbol
name="Victoria"

# When we want to access a variable, we need to use the $ symbol to reference it:
echo $name

# it’s good practice to wrap the variable name in curly brackets and encase it in double quotes
echo "${name}"